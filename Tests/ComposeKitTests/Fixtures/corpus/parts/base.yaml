# Reusable base services, pulled in via `extends` from extends-main.yaml.
services:
  base-app:
    image: app:1.0
    environment:
      LOG_LEVEL: info
      REGION: us-east-1
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "true"]
      interval: 10s
